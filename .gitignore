# frontend (Node / Vite)
node_modules/
dist/
dist-ssr/
.vite/
*.local

# OS
.DS_Store
Thumbs.db

# editor
.vscode/*
!.vscode/extensions.json

# Claude Code local settings (machine/session-specific)
.claude/settings.local.json

# NOTE: *.mp3 is intentionally NOT ignored — demo audio is committed to the repo.
